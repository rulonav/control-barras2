// src/styles/ScannerModalStyles.js
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  // Puedes agregar estilos específicos para ScannerModal si los necesita
  // Por ahora, un objeto vacío o estilos genéricos
});

// ✅ Exportar con nombre
export { styles };